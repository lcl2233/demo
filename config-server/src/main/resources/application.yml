server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/

spring:
  cloud:
    config:
      server:
          git:
            uri: 
            username: 
            password: 
            search-paths: spring-config
          overrides: #属性覆盖或者添加
                name: didi
security:
  user:
    name: user
    password: 1234567
